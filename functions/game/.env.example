# Copy to functions/game/.env and fill in. This file is committed; .env is gitignored.
# A server API key with scopes: databases.read, databases.write
# (Only needed as a fallback — Appwrite injects a dynamic key via the x-appwrite-key
#  header when the function declares scopes, so this is optional on recent versions.)
APPWRITE_API_KEY=
